<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Base application theme. Switched from Theme.AppCompat.Light.DarkActionBar
      to DayNight.NoActionBar because the game palette is dark — the previous
      light parent caused a visible white flash between splash dismissal and
      the WebView first paint on some Android devices.
    -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <!--
      Launch theme — used until the WebView is ready. Transparent system bars
      complement the edge-to-edge WebView set up in MainActivity so there are
      no visible bar-inset borders around the game.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
